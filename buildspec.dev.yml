version: 0.2

env:
    variables:
        TEST: 'TESTING VALUE'
    parameter-store:
        NODE_AUTH_TOKEN: 'node_auth_token'
phases:
    install:
        runtime-versions:
            nodejs: 12
        #install:
        #If you use the Ubuntu standard image 2.0 or later, you must specify runtime-versions.
        #If you specify runtime-versions and use an image other than Ubuntu standard image 2.0, the build fails.
        #runtime-versions:
        # name: version
        # name: version
        commands:
            - npm install yarn --global
            - echo ${NODE_AUTH_TOKEN}
            - echo ${TEST}
            - export NODED_AUTH_TOKEN=${NODE_AUTH_TOKEN} && yarn install --prod --pure-lockfile
        # - command
        #pre_build:
        #commands:
        # - command
        # - command
    build:
        commands:
            - yarn build
            # - command
        #post_build:
        #commands:
        # - command
        # - command
artifacts:
    files:
        - '**/*'
    base-directory: ./public
#reports:
#report-name-or-arn:
#files:
# - location
# - location
#base-directory: location
#discard-paths: yes
#file-format: JunitXml | CucumberJson
#files:
# - location
# - location
#name: $(date +%Y-%m-%d)
#discard-paths: yes
#base-directory: location
#cache:
#paths:
# - paths
