import { Icons } from '@assets';

export const ArrowDown = () => {
    return <img src={Icons.ArrowDown} />;
};

export const ArrowLeft = () => {
    return <img src={Icons.ArrowLeft} />;
};

export const ArrowRight = () => {
    return <img src={Icons.ArrowRight} />;
};
